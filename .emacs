;; Turn on the L# and C# on the mode line.
(line-number-mode 1)
(column-number-mode 1)

; set shortcut for goto-line
(global-set-key "\C-cg" 'goto-line)

; set shortcut for revert-file
(global-set-key "\C-cr" 'revert-buffer)
