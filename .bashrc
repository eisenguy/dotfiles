export EDITOR=emacs

# for bash completion of git and yum commands
source /etc/bash_completion.d/git
source /etc/bash_completion.d/yum
# include something here for apt completion?

# sometimes needed to fix issues with terminal display of smart quotes
LC_ALL=C

